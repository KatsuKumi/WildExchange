{% extends 'base.html.twig' %}
{% block body %}

    <div class="container-fluid smallpaddingtop">
    </div>
    <div class="container-fluid">
        <div class="row">
            {% for value in attributes %}
            {% endfor %}
            {% for value in attributes %}
                <a href="{{ path('questionpage', { 'tag': value.nom, page: 1 }) }}">

                <div class="col-md-6 col-sm-6 col-xs-12 ">
                        <div class="img-responsive" style="background-image: url({{ asset(value.imageurl) |replace({"\n":'', "\r":'', ' ':''})}});">
                            <a class="cadre-gauche" href="{{ path('questionpage', { 'tag': value.nom, page: 1 }) }}">QUESTIONS</a>
                            <div class="centertext">
                                {{ value.nom }}
                            </div>
                            <a href="{{ path('questionpage', { 'tag': value.nom, page: 1 }) }}" class="Cdroit">DOCUMENTATION</a>
                        </div>
                </div>
                </a>

            {% endfor %}
        </div>
    </div>

{% endblock %}

